<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Mon Profil</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600&display=swap" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/boxicons/css/boxicons.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/user.css">
  <link rel="stylesheet" href="/css/navigationuser.css">
  <style>
.profile-wrapper {
  max-width: 700px; /* plus large */
  background: #fff;
  border-radius: 10px;
  box-shadow: 0 2px 16px rgba(0,0,0,0.1);
  overflow: hidden; margin-left: 180px;
}

.cover-photo {
  width: 100%;
  height: 164px; /* plus haut */
  background: linear-gradient(90deg, #4e73df 60%, #6285f5 100%);
  position: relative;
}

.avatar-container {
  position: absolute;
  left: 50%;
  bottom: -80px; /* plus bas */
  transform: translateX(-50%);
  z-index: 2;
}

.avatar-initial {
  width: 160px; /* plus grand */
  height: 160px;
  background: #fff;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 5rem; /* plus grand */
  color: #4e73df;
  font-weight: 700;
  border: 6px solid #fff;
  box-shadow: 0 2px 12px rgba(78,115,223,0.15);
}

.profile-main {
  padding: 100px 48px 48px 48px; /* plus d’espace en haut et sur les côtés */
  text-align: center;
}

.profile-name {
  font-size: 3rem; /* plus grand */
  font-weight: 700;
  color: #222;
  margin-bottom: 12px;
  margin-top: 0;
}

.profile-role {
  color: #4e73df;
  font-weight: 600;
  font-size: 1.5rem; /* plus grand */
  margin-bottom: 24px;
}

.profile-info-list {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 16px; /* plus d’espace entre les lignes */
  margin-top: 24px;
}

.profile-info-row {
  font-size: 1.3rem; /* plus grand */
  color: #444;
  background: #f8f9fc;
  border-radius: 8px;
  padding: 12px 28px; /* plus de padding */
  min-width: 280px; /* plus large */
  text-align: left;
  box-shadow: 0 1px 3px rgba(78,115,223,0.05);
}

.profile-info-label {
  color: #6285f5;
  font-weight: 600;
  margin-right: 12px;
}

.profile-solde {
  color: #4e73df;
  font-weight: 700;
}

/* Responsive */
@media (max-width: 700px) {
  .profile-wrapper {
    max-width: 95vw;
  }
  .profile-main {
    padding: 100px 5vw 32px 5vw;
  }
  .avatar-initial {
    width: 120px;
    height: 120px;
    font-size: 3.5rem;
  }
  .cover-photo {
    height: 180px;
  }
  .profile-name {
    font-size: 2.2rem;
  }
  .profile-role {
    font-size: 1.2rem;
  }
  .profile-info-row {
    font-size: 1.1rem;
    min-width: auto;
    padding: 10px 18px;
  }
}
    .profile-info-label {
      color: #6285f5;
      font-weight: 500;
      margin-right: 8px;
    }
    .profile-solde {
      color: #4e73df;
      font-weight: 700;
    }
    @media (max-width: 600px) {
      .profile-wrapper { max-width: 98vw; }
      .profile-main { padding: 80px 4vw 24px 4vw; }
      .avatar-initial { width: 80px; height: 80px; font-size: 2.2rem; }
      .cover-photo { height: 120px; }
    }
  </style>
</head>
<body>
  <%- include('./navbar') %>
<div class="main-content">
  <div class="profile-wrapper">
    <div class="cover-photo">
      <div class="avatar-container">
        <div class="avatar-initial">
          <%= user.nom.charAt(0).toUpperCase() %>
        </div>
      </div>
    </div>
    <div class="profile-main">
      <h1 class="profile-name"><%= user.nom %></h1>
      <div class="profile-role"><%= user.poste %></div>
      <div class="profile-info-list">
        <div class="profile-info-row">
          <span class="profile-info-label">Email :</span>
          <span><%= user.email %></span>
        </div>
        <div class="profile-info-row">
          <span class="profile-info-label">Rôle :</span>
          <span><%= user.role %></span>
        </div>
        <div class="profile-info-row">
          <span class="profile-info-label">Solde de congés :</span>
          <span class="profile-solde"><%= user.solde_conges %> jours</span>
        </div>
      </div>
    </div>
  </div>
</div>
<script src="/js/animationnav.js"></script>
<script src="/js/baractivation.js"></script>
</body>
</html>
